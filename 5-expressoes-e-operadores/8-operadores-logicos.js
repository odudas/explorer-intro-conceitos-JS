// Operadores lógicos (logical operators)

// - 2 valores booleanos, quando verificados,
// resultará em verdadeiro ou falso

let pao = true;
let queijo = true;

// AND &&   => 1 E outro é true
console.log(pao && queijo);

// OR ||    => 1 OU outro é true
console.log(pao || queijo);

// NOT !    => inverte o valor
console.log(!pao);
